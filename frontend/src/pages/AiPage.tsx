import { useState } from 'react';
import { api } from '../services/api';
import { PageHeader, SectionCard, StatusBadge, primaryButton } from '../components/ErpUi';
type Action = { area: string; priority: 'alta' | 'media' | 'baixa'; title: string; rationale: string; recommended_action: string };
type Analysis = { headline: string; summary: string; actions: Action[]; budget_suggestions: { title: string; suggestion: string }[]; content_ideas: { title: string; format: string; angle: string }[] };
const scopes = [['operacao','Operação'],['financeiro','Financeiro'],['comercial','Comercial'],['marketing','Marketing'],['site','Site'],['clientes','Clientes'],['propostas','Propostas']];
export default function AiPage() { const [scope,setScope]=useState('operacao'); const [instructions,setInstructions]=useState(''); const [analysis,setAnalysis]=useState<Analysis|null>(null); const [loading,setLoading]=useState(false); const [error,setError]=useState(''); async function generate(){setLoading(true);setError('');try{const {data}=await api.post('/ai/analyze',{scope,instructions});setAnalysis(data.analysis)}catch(e:any){setError(e.response?.data?.detail||'Não foi possível gerar a análise. Entre na plataforma e confirme a configuração do Groq.')}finally{setLoading(false)}} return <div className="mx-auto max-w-7xl space-y-6"><PageHeader eyebrow="Assistente IA" title="Análises e próximos passos" description="Transforme os dados do ERP e do site em prioridades, orçamento e ideias de conteúdo." action={<button onClick={generate} disabled={loading} className={`${primaryButton} disabled:opacity-60`}>{loading?'Analisando…':'Gerar análise'}</button>}/><SectionCard title="Contexto da análise" subtitle="Escolha uma área e descreva o que precisa decidir."><div className="flex flex-wrap gap-2">{scopes.map(([id,label])=><button key={id} onClick={()=>setScope(id)} className={`rounded-xl px-3 py-2 text-sm ${scope===id?'bg-violet-500 text-white':'bg-white/5 text-slate-300'}`}>{label}</button>)}</div><textarea value={instructions} onChange={e=>setInstructions(e.target.value)} className="mt-5 min-h-24 w-full rounded-2xl border border-white/10 bg-slate-950/40 p-4 text-sm text-white" placeholder="Ex.: sugira orçamento, posts para a semana, prioridades comerciais ou melhorias de conversão." /></SectionCard>{error&&<div className="rounded-2xl bg-rose-500/10 p-4 text-sm text-rose-100">{error}</div>}{analysis?<><SectionCard title={analysis.headline} subtitle={analysis.summary}><div className="space-y-4">{analysis.actions?.map(a=><article key={a.title} className="rounded-2xl bg-violet-500/10 p-5"><div className="flex justify-between gap-3"><div><p className="text-xs uppercase text-violet-200">{a.area}</p><p className="mt-2 font-semibold text-white">{a.title}</p></div><StatusBadge tone={a.priority==='alta'?'rose':a.priority==='media'?'amber':'emerald'}>{a.priority}</StatusBadge></div><p className="mt-3 text-sm text-slate-300">{a.rationale}</p><p className="mt-3 text-sm text-violet-100">Próximo passo: {a.recommended_action}</p></article>)}</div></SectionCard><div className="grid gap-6 lg:grid-cols-2"><SectionCard title="Sugestões orçamentárias" subtitle="Estimativas gerenciais; valide antes de executar">{analysis.budget_suggestions?.map(x=><div key={x.title} className="mb-3 rounded-2xl bg-slate-950/30 p-4"><p className="font-medium text-white">{x.title}</p><p className="mt-2 text-sm text-slate-300">{x.suggestion}</p></div>)}</SectionCard><SectionCard title="Ideias de conteúdo" subtitle="Posts e campanhas sugeridos">{analysis.content_ideas?.map(x=><div key={x.title} className="mb-3 rounded-2xl bg-slate-950/30 p-4"><p className="font-medium text-white">{x.title}</p><p className="mt-1 text-xs text-violet-200">{x.format}</p><p className="mt-2 text-sm text-slate-300">{x.angle}</p></div>)}</SectionCard></div></>:<SectionCard title="Use a IA em cada módulo" subtitle="Financeiro para orçamento e caixa; Comercial para follow-ups; Marketing para posts; Site para conversão; Operação para prioridades integradas."><p className="text-sm text-slate-400">Os dados disponíveis são usados como contexto e a IA informa quando ainda falta uma fonte conectada.</p></SectionCard>}</div> }